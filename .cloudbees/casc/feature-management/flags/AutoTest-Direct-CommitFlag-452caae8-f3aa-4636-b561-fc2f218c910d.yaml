apiVersion: featuremanagement.cloudbees.io/v1alpha1
      kind: flag

      flag: AutoTest-Direct-CommitFlag-452caae8-f3aa-4636-b561-fc2f218c910d
      flagType: number
      description: "Flag created via direct commit"
      labels:

      availableValues:

      isPermanent: false